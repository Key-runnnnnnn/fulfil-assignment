# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
APP_NAME=Product Importer
DEBUG=True
API_V1_PREFIX=/api/v1

# ============================================================================
# POSTGRESQL DATABASE CONFIGURATION
# ============================================================================
# Individual database credentials (recommended for clarity)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=product_importer

# OR use full DATABASE_URL (this will override individual settings above)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/product_importer

# ============================================================================
# NOTES:
# ============================================================================
# - Copy this file to .env and update with your actual credentials
# - Never commit .env file to version control
# - DATABASE_URL takes precedence over individual DB_* variables if both are set
